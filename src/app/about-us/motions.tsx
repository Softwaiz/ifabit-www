"use client";
import { useLayoutEffect } from "react";
import { scroll, animate } from "motion";

export function AboutPageMotions() {
  /*useLayoutEffect(() => {
document.querySelectorAll(".feature").forEach((section) => {
      console.log(section);

      scroll(animate(section, { y: [-400, 400] }), {
        target: section,
      });

    });
  }, []);*/

  return <></>;
}
